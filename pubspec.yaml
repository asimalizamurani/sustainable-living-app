name: sustainable_living_app
description: "A comprehensive sustainable living application with role-based access."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.6.2

dependencies:
  cloud_firestore: ^5.6.10
  crypto: ^3.0.6
  firebase_core: ^3.14.0
  firebase_auth: ^5.3.3
  image_picker: ^1.0.7
  flutter:
    sdk: flutter
  flutter_pw_validator: ^1.6.0
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  fl_chart: ^0.69.0
  url_launcher: ^6.2.5
  permission_handler: ^11.3.1
  geolocator: ^12.0.0
  flutter_local_notifications: ^17.2.2

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
